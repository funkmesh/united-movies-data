{
  "name": "inflight-movies-tools",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Builds enriched inflight catalog feeds for US airlines (United, American, Delta), published to GitHub Pages.",
  "scripts": {
    "build": "node build-catalog.mjs",
    "flight": "node flight-mode.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "puppeteer": "^23.11.1"
  }
}
